import { UATDraggableDirective } from './draggable.directive';

describe('UATDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new UATDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
