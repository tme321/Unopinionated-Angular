import { UATDragAndDropContainerComponent } from './drag-and-drop-container.component';

describe('UATDragAndDropContainerComponent', () => {
  it('should create an instance', () => {
    const directive = new UATDragAndDropContainerComponent();
    expect(directive).toBeTruthy();
  });
});
